[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "repairgraph-commons"
version = "0.1.0"
description = "Open, validated symptom-cause-part-repair knowledge graph tools"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Kwondh0321" }]
keywords = ["repair", "right-to-repair", "knowledge-graph", "diagnostics", "open-data"]

[project.optional-dependencies]
dev = ["ruff==0.16.4"]

[project.urls]
Homepage = "https://github.com/Kwondh0321/repairgraph-commons"
Repository = "https://github.com/Kwondh0321/repairgraph-commons.git"
Issues = "https://github.com/Kwondh0321/repairgraph-commons/issues"

[project.scripts]
repairgraph = "repairgraph.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
